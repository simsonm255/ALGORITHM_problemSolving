/**
 * @param {number[]} nums
 * @param {number} target
 * @return {number}
 */
var t;

var search = function(nums, target) {
    // t=target;
    var find = x=>x==target;
    
    return nums.findIndex(find);

};

 